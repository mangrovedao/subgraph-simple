{
  "name": "mangrove",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph codegen && graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ mangrove",
    "create-local": "graph create --node http://localhost:8020/ mangrove",
    "remove-local": "graph remove --node http://localhost:8020/ mangrove",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 mangrove",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.60.0",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
